CREATE TABLE INVENTARIO
(
ID_INVENTARIO INT NOT NULL,
DESCRIPCION varchar2(200),
COSTO FLOAT,
PRECIO FLOAT
);

create or replace PROCEDURE INSPROCEDURE(PIDINVENTARIO IN INVENTARIO.ID_INVENTARIO%TYPE, PDESCRIPCION IN INVENTARIO.DESCRIPCION%TYPE, 
PCOSTO IN INVENTARIO.COSTO%TYPE, PPRECIO IN INVENTARIO.PRECIO%TYPE)
AS
BEGIN 
INSERT INTO INVENTARIO VALUES (PIDINVENTARIO,PDESCRIPCION,PCOSTO,PPRECIO);
END insprocedure;
